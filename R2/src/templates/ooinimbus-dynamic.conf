[defaults]

iaas: Nimbus
iaas_key: ${iaas_key}
iaas_secret: ${iaas_secret}
iaas_url: ${iaas_url}

image: ${image}
allocation: ${allocation}
sshkeyname: ${sshkeyname}
localsshkeypath: ~/.ssh/id_rsa
ssh_username: root
scp_username: epu

[globals]
${globals}

[runlevels]

level1: basenode/basenode-ooinimbus-dynamic.conf
level2: pyonconfig/pyon.conf
level3: pyon-bootstrap/pyon-bootstrap-vm.conf
level4: dtrs/dtrs.conf
level5: dtrs-bootstrap/dtrs-bootstrap.conf
level6: provisioner/provisioner.conf
level7: epum/epum.conf
level8: pd/pd.conf
level9: pd-bootstrap/pd-bootstrap.conf

# Do not modify after this line. Pyon service runlevels are autogenerated
# with the rel2levels script
########## Pyon Services ##########
${levels}
