{
  "server":{
    "amqp":{
      "host": "${rabbitmq_host}",
      "vhost": "/",
      "username": "${rabbitmq_username}",
      "password": "${rabbitmq_password}",
      "exchange": "${exchange_scope}"
    }
  },
  "dashi":{
    "exchange": "${exchange_scope}"
  },
  "run_type": "supd",
  "parameters": {
    "exec": "touch",
    "argv": ["/tmp/hellofromprocess"]
  }
}
