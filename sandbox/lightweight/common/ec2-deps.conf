[deps]
# Python installation paths
python: python
virtualenv: virtualenv
# cloudinitd won't allow specifying an empty value here. --quiet should be harmless.
virtualenv_args: --quiet
coi_services_egg_cache: https://github.com/downloads/ooici/dt-data/coi-services-ubuntu-cache.tar.gz
ctxbroker_host: ${basenode.hostname}
