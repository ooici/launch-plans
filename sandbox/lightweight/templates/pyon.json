{
  "run_type": "pyon_single",
  "parameters":{                                                                
    "apps":${app_json}                                                                   
  },  
  "server":{
    "amqp":{
      "host": "$${rabbitmq_host}",
      "vhost": "/",
      "username": "$${rabbitmq_username}",
      "password": "$${rabbitmq_password}",
      "exchange": "$${exchange_scope}"
    }
  }
}
