{
  "server": {
    "amqp": {
      "host": "${rabbitmq_host}",
      "username": "${rabbitmq_username}",
      "password": "${rabbitmq_password}"
    },
    "couchdb": {
      "host": "${couchdb_host}",
      "username": "${couchdb_username}",
      "password": "${couchdb_password}"
    }
  },
  "epu_username": "${epu_username}"
}
