{
  "server":{
    "couchdb":{
      "username": "${couchdb_username}",
      "password": "${couchdb_password}"
    }
  },
  "system":{
    "force_clean":"true"
  },
  "coi_services_system_name": "${coi_services_system_name}",
  "cleancouchdb": "${cleancouchdb}"
}
